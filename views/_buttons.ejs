<div class="button-wrapper">
    <a href="/" class="logo">SastaBin</a>
    <div class="buttons">
        <% if(locals.canSave) { %>
        <button type="submit" class="btn">Save</button>
        <% } %>
        <% if(locals.id != null) { %>
            <a href= "/<%= locals.id %>/duplicate" class="btn">Duplicate</a>
        <% } %>
        <a href= "/new" class="btn">New</a>
    </div>
</div> 